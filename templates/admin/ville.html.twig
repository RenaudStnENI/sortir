{% extends ('layout.html.twig') %}

{% block title %}{{ parent() }} mon Profil{% endblock %}

{% block main %}


    <div class="container">
        <div class="row">

            <div class="col-sm-3">
            </div>

            <div class="col-sm-6">

                <div class="container">

                    <p>
                    <h1>Liste des villes</h1></p>
                    <br>

                    <table class="table">

                        {% for ville in villes %}

                            <tr>
                                <td>{{ ville.nom }}</td>
                                <td><a href="">modifier</a></td>
                                <td> |</td>
                                <td><a href="">supprimer</a></td>
                            </tr>

                        {% endfor %}


                    </table>
                    <div>
                        <a href="{{ path('list') }}"> <button class="btn btn-primary" >retour</button></a>
                    </div>
                </div>
            </div>

        </div>
        <div class="col-sm-3">


        </div>
    </div>

{% endblock %}