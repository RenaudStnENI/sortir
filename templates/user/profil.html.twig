{% extends ('layout.html.twig') %}

{% block title %}{{ parent() }} mon Profil{% endblock %}

{% block main %}

    <div class="container">
        <div class="row">

            <div class="col-sm-2">
            </div>

            <div class="col-sm-8">
                <div class="jumbotron">

                    <div class="container"><img src="{{ userDetail.photo }}" width="200px" height="200px"></div>

                    <p><h1>Profil de {{ userDetail.username}}</h1></p>

                    <div>
                        <p>Prénom :</p>
                        <p><h3> {{ userDetail.prenom }} </h3></p>
                    </div>

                    <div>
                        <p>Nom :</p>
                        <p><h3> {{ userDetail.nom }}</h3></p>
                    </div>

                    <div>
                        <p>E-mail :</p>
                        <p><h3>  {{ userDetail.mail }}</h3></p>
                    </div>

                    <div>
                        <p>Site :</p>
                        <p><h3>{{ userDetail.site.nom }}</h3> </p>
                    </div>

                </div>
            </div>

            <div class="col-sm-2">
            </div>


        </div>
    </div>



{% endblock %}